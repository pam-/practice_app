<% if signed_in? %> 
	<%= form_for(Comment.new) do |f| %> 
		<%= f.hidden_field :post_id, value: @post.id %>
		<%= f.hidden_field :user_id, value: current_user.id %> 
		<%= f.text_area :content, placeholder: "Be blunt" %>
		<%= f.submit 'Submit', class: "small-button" %> 
	<% end %> 
<% end %> 